import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color.fromRGBO(241, 208, 24, 1);

  static const Color secondary = Color.fromRGBO(7, 12, 105, 1);

  static const Color foreground = Color(0xFF000000);

  static const Color mutedForeground = Color.fromRGBO(114, 114, 114, 1);

  static const Color links = Color.fromRGBO(112, 88, 255, 1);

  static const Color background = Color.fromRGBO(237, 237, 237, 1);
  
  static const Color white = Colors.white;
}